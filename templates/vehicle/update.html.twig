{% extends 'base.html.twig' %}

{% block title %}Modification de véhicule{% endblock %}

{% block body %}

    <div class="container">
        <h2>Modifier la {{ vehicle.model.brand.name }} {{ vehicle.model.name }}</h2>

        {{ form_start(form) }}
            {{ form_row(form.capacity) }}
            {{ form_row(form.price) }}
            {{ form_row(form.numberKilometers) }}
            {{ form_row(form.numberPlate) }}
            {{ form_row(form.yearOfVehicle) }}
            {{ form_row(form.picturePath) }}
            {{ form_row(form.type) }}
            {{ form_row(form.model) }}
            {{ form_row(form.vehicleOption) }}
            {{ form_row(form.submit, {'label': 'Modifier', 'attr': {'placeholder' : 'Modifier'}}) }}
        {{ form_end(form) }}

        <a href="/vehicle/update" class="btn btn-primary">Retour</a>
    </div>

{% endblock %}